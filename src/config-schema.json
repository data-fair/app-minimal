{
  "type": "object",
  "required": ["datasets"],
  "properties": {
    "datasets": {
      "type": "array",
      "items": [{
        "title": "Jeu de données",
        "type": "object",
        "x-fromUrl": "api/v1/datasets?status=finalized&q={q}",
        "x-itemsProp": "results",
        "x-itemTitle": "title",
        "x-itemKey": "href",
        "properties": {
          "href": {"type": "string"},
          "title": {"type": "string"},
          "page": {"type": "string"}
        }
      }]
    }
  }
}
